version: '3.8'

services:
  fastapi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./results:/app/src/fastApi/results
      - ./data:/app/src/fastApi/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped 